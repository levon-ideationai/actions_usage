on: [push]

jobs:
  custom_test:
    runs-on: ubuntu-latest
    name: We test it locally with act
    steps:
        - name: Levon Dummy Action
          uses: levon-ideationai/actions@v02
          id: pokemon
          with:
            pokemon_id: 150
